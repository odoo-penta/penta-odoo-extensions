<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Terminos de pago -->
    <record id="view_payment_term_form_inherit_penta" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.pentalab</field>
        <field name="model">account.payment.term</field>
        <field name="inherit_id" ref="account.view_payment_term_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group/field[@name='company_id']" position="after">
                <field name="generate_installments"/>
                <field name="installments_number" invisible="not generate_installments"/>
            </xpath>
        </field>
    </record>

</odoo>
