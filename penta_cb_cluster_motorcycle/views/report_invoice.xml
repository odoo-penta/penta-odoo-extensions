<?xml version="1.0" encoding="utf-8"?>
<odoo>

<template id="report_invoice_document_edi_inherit_motorcycle"
          inherit_id="l10n_ec_edi.report_invoice_document">

    <!-- ✔ Insertar columna extra antes de DESCRIPTION -->
    <xpath expr="//table[@name='invoice_line_table']//th[@name='th_description']"
           position="before">

        <th name="th_extra_motorcycle" class="text-start">
            <span>Detalles</span>
        </th>

    </xpath>

    <!-- ✔ Insertar datos antes de DESCRIPTION en el TBODY -->
    <xpath expr="//table[@name='invoice_line_table']//td[@name='account_invoice_line_name']"
           position="before">

        <td name="td_extra_motorcycle" class="text-start" style="font-size:12px;">

            <t t-if="line.move_id.stock_lot_ids">

                <t t-set="attrs" t-value="[]"/>
                <t t-set="ramv" t-value="''"/>
                <t t-set="motor" t-value="''"/>
                <t t-set="lote" t-value="''"/>

                <!-- Atributos -->
                <t t-foreach="line.product_id.product_template_attribute_value_ids" t-as="attr">
                    <t t-set="attrs" t-value="attrs + ['%s: %s' % (attr.attribute_id.name, attr.name)]"/>
                </t>

                <!-- Marca -->
                <t t-if="line.product_id.product_brand_id">
                    <t t-set="attrs" t-value="attrs + ['Marca: %s' % line.product_id.product_brand_id.name]"/>
                </t>

                <!-- Lotes -->
                <t t-foreach="line.move_id.stock_lot_ids" t-as="lot">
                    <t t-if="lot.product_id.id == line.product_id.id">
                        <t t-if="not ramv"><t t-set="ramv" t-value="lot.ramv or ''"/></t>
                        <t t-if="not motor"><t t-set="motor" t-value="lot.motor_number or ''"/></t>
                        <t t-if="not lote"><t t-set="lote" t-value="lot.name or ''"/></t>
                    </t>
                </t>

                <span t-esc="', '.join(attrs)
                        + ((', Ramv: ' + ramv) if ramv else '')
                        + ((', Motor: ' + motor) if motor else '')
                        + ((', Chasis: ' + lote) if lote else '')"/>
            </t>

        </td>

    </xpath>

</template>


    <template id="invoice_tbody_inherti_edi" inherit_id="account.report_invoice_document">
        <xpath expr="//tbody[@class='invoice_tbody']" position="replace">
            <t t-set="lotes_linea_usados" t-value="[]"/>
            <tbody class="invoice_tbody">
                <t t-set="current_subtotal" t-value="0"/>
                <t t-set="current_total" t-value="0"/>
                <t t-set="lines" t-value="o.invoice_line_ids.sorted(key=lambda l: (-l.sequence, l.date, l.move_name, -l.id), reverse=True)"/>

                <t t-foreach="lines" t-as="line">
                    <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal"/>
                    <t t-set="current_total" t-value="current_total + line.price_total"/>

                    <tr t-att-class="'fw-bold o_line_section' if line.display_type == 'line_section' else 'fst-italic o_line_note' if line.display_type == 'line_note' else ''">

                        <!-- PRODUCTO -->
                        <t t-if="line.display_type == 'product'" name="account_invoice_line_accountable">

                            <!-- Recorremos la cantidad -->
                            <t t-set="qty_units" t-value="list(range(int(line.quantity or 0)))"/>

                            <t t-foreach="qty_units" t-as="u_index">
                                <tr>
                                    <td name="account_invoice_line_name">
                                        <span t-if="line.name" t-field="line.name" t-options="{'widget': 'text'}"/>
                                    </td>

                                    <!-- Cantidad fija a 1 por unidad -->
                                    <td name="td_quantity" class="o_td_quantity text-end">
                                        <span class="text-nowrap">1</span>
                                        <span t-field="line.product_uom_id" groups="uom.group_uom"/>
                                    </td>

                                    <!-- Precio unitario -->
                                    <td name="td_price_unit" t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                        <span class="text-nowrap" t-field="line.price_unit"/>
                                    </td>

                                    <!-- Descuento -->
                                    <td name="td_discount" t-if="display_discount" t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                        <span class="text-nowrap" t-field="line.discount"/>
                                    </td>

                                    <!-- Impuestos -->
                                    <t t-set="taxes" t-value="', '.join([(tax.invoice_label or tax.name) for tax in line.tax_ids])"/>
                                    <td name="td_taxes"
                                        t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }} {{ 'text-nowrap' if len(taxes) &lt; 10 else '' }}">
                                        <span t-out="taxes" id="line_tax_ids"/>
                                    </td>

                                    <!-- Subtotal por unidad -->
                                    <td name="td_subtotal" class="text-end o_price_total">
                                        <span class="text-nowrap"
                                            t-out="'%.2f' % (line.price_subtotal / (line.quantity or 1))"/>
                                    </td>

                                </tr>
                            </t>
                        </t>

                        <!-- SECCIÓN -->
                        <t t-elif="line.display_type == 'line_section'">
                            <td colspan="99">
                                <span t-if="line.name" t-field="line.name" t-options="{'widget': 'text'}"/>
                            </td>
                            <t t-set="current_section" t-value="line"/>
                            <t t-set="current_subtotal" t-value="0"/>
                        </t>

                        <!-- NOTA -->
                        <t t-elif="line.display_type == 'line_note'">
                            <td colspan="99">
                                <span t-if="line.name" t-field="line.name" t-options="{'widget': 'text'}"/>
                            </td>
                        </t>

                    </tr>

                    <!-- SUBTOTAL DE SECCIÓN -->
                    <t t-if="current_section and (line_last or lines[line_index+1].display_type == 'line_section')">
                        <tr class="is-subtotal text-end">
                            <td colspan="99">
                                <strong class="mr16">Subtotal</strong>
                                <span
                                    t-out="current_subtotal"
                                    t-options='{"widget": "monetary", "display_currency": o.currency_id}'/>
                            </td>
                        </tr>
                    </t>

                </t>
            </tbody>
        </xpath>
    </template>

</odoo>